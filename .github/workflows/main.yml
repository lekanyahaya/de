name: first ci/cd project

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: checkout code
      uses: actions/checkout@v3
    
    - name: greeting
      run: echo good morning fam
    
