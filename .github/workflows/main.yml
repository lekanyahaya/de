name: first ci/cd project

on: 
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on:ubuntu-latest
    steps:
    
    uses: actions/checkout@v3
      - name: greeting
        run: echo good morning fam
    
